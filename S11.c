// 비교, 논리 연산자
#include <stdio.h>
#include <stdbool.h> // bool은 Cpp에서만 기본형이라 C에선 stdbool.h 사용해야함
int main(void) {
  int a, b;
  scanf("%d %d", &a, &b);
  bool p = a > b;
  bool q = a < b;
  bool r = a == b; // == 같다.

  // bool : 진리값을 저장용 변수 (1바이트)
  // >=, <=, !=

  /* 논리연산자 */
  /*
  && : 그리고
  || : or 또는
  ! : not
  */

 
}